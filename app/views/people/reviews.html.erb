<%= render 'social/user_profile_header', person: @person %>
<%= render partial: 'social/social_nav', locals: profile_header_porps(@person)  %>

<% if reviews.size > 0 %>
    <div id="profile-testimonials-list">
      <%= render :partial => 'testimonials', :locals => {:received_testimonials => reviews, :limit => 15} %>
    </div>
<% end %>