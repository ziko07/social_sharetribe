.checkbox-container
  = check_box_tag "person[preferences][#{notification_type}]", "true", target_user.preferences[notification_type] ? true : false, :id => notification_type, :data_group_cls => "btn-group-sm"
  = label_tag notification_type, t("settings.notifications.#{notification_type}")
